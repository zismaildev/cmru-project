# 📝 Assignment 6: Sets & Dictionaries

แบบฝึกหัดการใช้งานโครงสร้างข้อมูลแบบ Set และ Dictionary (Key-Value Pair)

## ✏️ โจทย์ (Problems)
1. **Unpacking**: การกระจายค่าจาก List ลงตัวแปร x, y, z
2. **Set Creation**: การสร้าง Set จาก List หรือค่าคงที่
3. **Set Operations**:
   - `add()`, `remove()`, `pop()`, `clear()`
   - **Math**: `Union`, `Intersection`, `Difference`
4. **Dictionary Basics**: การประกาศและวนลูปอ่าน Key/Value
5. **Dictionary Methods**:
   - `.keys()`, `.values()`, `.items()`
   - `.get()`, `.pop()`, `.update()`, `.clear()`
6. **Student DB**: โปรแกรมจัดการข้อมูลนักศึกษา (CRUD) ด้วย Dictionary

## 🚀 ผลลัพธ์ (Key Concepts)
- **Set**: ข้อมูลที่ไม่ซ้ำกันและไม่เรียงลำดับ
- **Dictionary**: การเก็บข้อมูลแบบ Key-Value เหมาะสำหรับทำ Database เล็กๆ
