# 🧪 Practical Test 2

แบบทดสอบเก็บคะแนนครั้งที่ 2 (List & Menu Loop)

## 📝 โจทย์ (Problems)
เขียนโปรแกรมเมนู (Menu-Driven Program) ที่ทำงานวนลูปจนกว่าผู้ใช้จะกด 9 เพื่อออก

### เมนูการทำงาน
1. **ป้อนตัวเลข**: รับค่าจำนวนเต็มเก็บลงใน List (`append`)
2. **เรียงข้อมูล**: แสดงข้อมูลใน List โดยเรียงจากมากไปน้อย (`sorted(reverse=True)`)
3. **แสดงข้อมูล**: แสดงข้อมูลทั้งหมดใน List แบบปกติ
9. **ออกจากโปรแกรม**: จบการทำงาน (`break`)

## 🚀 ผลลัพธ์ (Key Concepts)
- `while True` loop for menu
- List operations (`append`)
- Sorting (`sorted`)
- Global variables usage
