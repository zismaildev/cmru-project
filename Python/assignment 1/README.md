# 📝 Assignment 1: Basic Input/Output

แบบฝึกหัดทบทวนการแสดงผล (Print) และการใช้ตัวแปร (Variables) เบื้องต้น

## ✏️ โจทย์ (Problems)
1. **Personal Info**: แสดงรหัส, ชื่อ, และจังหวัด
2. **Age Display**: แสดงอายุ
3. **Triangle Area**: คำนวณพื้นที่สามเหลี่ยมจากฐานและสูง (กำหนดค่าในตัวแปร)
4. **Rectangle Area**: คำนวณพื้นที่สี่เหลี่ยมผืนผ้า
5. **Variable Naming**: ตรวจสอบความถูกต้องของการตั้งชื่อตัวแปร
6. **Data Types**: การแสดงชนิดตัวแปร (`type`) และการแปลงค่า (`int`, `str`)

## 🚀 ผลลัพธ์ (Key Concepts)
- `print()` function
- Arithmetic Operators (`*`)
- Type Conversion (`int()`, `str()`)
