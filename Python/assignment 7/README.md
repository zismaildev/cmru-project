# 📝 Assignment 7: Functions

แบบฝึกหัดการสร้างฟังก์ชัน (User-Defined Functions) เพื่อการใช้งานซ้ำและการจัดระเบียบโค้ด

## ✏️ โจทย์ (Problems)
1. **Basic Function**: ฟังก์ชันแสดงตัวเลข `DispNum(num)`
2. **Sum Function**: ฟังก์ชันหาผลรวม `SumNum(num)`
3. **Calculator Menu**: เมนูเลือกบวก ลบ คูณ หาร โดยแยกเป็นฟังก์ชันย่อย (`add`, `sub`, `mul`)
4. **Parameters**: การส่งค่า Input เข้าฟังก์ชัน
5. **Return Values**: การส่งค่าผลลัพธ์กลับจากฟังก์ชัน
6. **Even/Odd Check**: ฟังก์ชันตรวจสอบเลขคู่คี่
7. **Geometry**: ฟังก์ชันคำนวณพื้นที่สามเหลี่ยม
8. **Advanced Args**:
   - `*args`: รับพารามิเตอร์จำนวนไม่จำกัด
   - `**kwargs`: รับพารามิเตอร์แบบ Key-Value ไม่จำกัด

## 🚀 ผลลัพธ์ (Key Concepts)
- **Modularity**: การแบ่งโปรแกรมใหญ่เป็นส่วนย่อยๆ
- **Scope**: ความเข้าใจเรื่องตัวแปร Global/Local
- **Arguments**: การส่งค่าแบบต่างๆ
