# 🏷️ Buy 2 Get 1 Free (Quiz)

โปรแกรมคำนวณโปรโมชั่นซื้อ 2 แถม 1 (แถมชิ้นที่ราคาถูกที่สุด)

## 📄 Source Code
**File:** [`quzi.dart`](./quzi.dart)

### 💡 Logic Concept
1. รับข้อมูลราคาสินค้าใส่ `List` ไปเรื่อยๆ จนกว่าจะพิมพ์ `-1`
2. หา **ราคาน้อยที่สุด (Min Price)** ในรายการ
3. นำราคารวมทั้งหมด ลบด้วย ราคาน้อยที่สุด = **ราคาที่ต้องจ่าย**

### 💻 Code Logic
```dart
// วนลุปรับค่า
while (true) {
  // ... (code รับค่า)
  if (code == -1) break;
  codes.add(code);
  prices.add(price);
}

// หาค่าต่ำสุด (Free Item Logic)
int minPrice = prices[0];
for (int i = 1; i < prices.length; i++) {
  if (prices[i] < minPrice) {
    minPrice = prices[i];
    minIndex = i; // เก็บตำแหน่งสินค้าที่แถม
  }
}

int finalPrice = totalPrice - minPrice;
```
