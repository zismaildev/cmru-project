# 📝 Chapter 2: Inputs & Interactivity

บทเรียนเกี่ยวกับการรับค่าจากผู้ใช้ (User Input) และการแสดงผลโต้ตอบผ่าน Dialogs

## ✨ ฟีเจอร์ที่เรียนรู้ (Features)

- **Text Fields (`TextFormField`)**: ช่องกรอกข้อความพร้อม Label และ Hint
- **Controllers (`TextEditingController`)**: การดึงค่าจากช่องกรอกข้อความ
- **Radio Buttons (`Radio`)**: ตัวเลือกเพศ (Male/Female)
- **Validation**: การตรวจสอบค่าว่างก่อนส่งข้อมูล
- **Dialogs (`AlertDialog`)**: 
  - แจ้งเตือนเมื่อกรอกข้อมูลไม่ครบ
  - แสดงข้อมูลต้อนรับเมื่อลงทะเบียนสำเร็จ

## 🛠️ Widgets สำคัญ
- `Scaffold`, `AppBar`: โครงสร้างหน้า
- `Column`, `Row`: การจัด Layout
- `TextFormField`: รับค่า Text
- `Radio`: ปุ่มเลือกตัวเลือกเดียว
- `ElevatedButton`: ปุ่มกดส่งข้อมูล
- `showDialog`: คำสั่งเรียกหน้าต่างแจ้งเตือน

## 🚀 วิธีการรัน (How to Run)
รันเป็น Flutter App ได้ดังนี้:

```bash
flutter run -t lib/learning/chapter 2/main.dart
```
(หรือใช้ VS Code กด F5 ที่ไฟล์ main.dart)
