# 🍽️ Menu (Workshop)

โปรเจกต์นี้เป็น Workshop สำหรับฝึกการสร้าง **UI รายการอาหาร (Food Menu)** ด้วย Flutter โดยเน้นการจัด Layout ที่สวยงามและการใช้ Widget พื้นฐานต่างๆ

## ✨ ฟีเจอร์หลัก (Features)
- **รายการอาหารแบบเลื่อนได้ (Scrollable List)**: ใช้ `ListView.builder` เพื่อแสดงรายการอาหารจำนวนมากอย่างมีประสิทธิภาพ
- **การ์ดอาหาร (Food Card)**: การออกแบบ Card ที่สวยงาม ประกอบด้วย:
  - 🖼️ รูปภาพอาหารเต็มกรอบ (Full-width Image)
  - 🏷️ ป้ายราคาโปร่งแสง (Translucent Price Tag)
  - 🇹🇭 ชื่ออาหารภาษาไทยและอังกฤษ
  - ⭐ ดาวรีวิว (Rating) และระดับความเผ็ด
- **การตกแต่ง (Styling)**: ใช้ Gradient Background สีส้ม-ขาว เพิ่มความพรีเมียม

## 🛠️ Widgets ที่ใช้เรียนรู้
- `MaterialApp` & `Scaffold`: โครงสร้างหลักของแอพ
- `ListView.builder`: การสร้างลิสต์ข้อมูลแบบ Dynamic
- `Card` & `Container`: การสร้างกรอบและตกแต่งพื้นหลัง
- `Stack` & `Positioned`: การวางเเลเยอร์ซ้อนทับ (เช่น ป้ายราคาบนรูปภาพ)
- `ClipRRect`: การตัดมุมภาพให้โค้งมน
- `LinearGradient`: การไล่เฉดสีพื้นหลัง

## 🚀 วิธีการรัน (How to Run)
เนื่องจากไฟล์นี้เป็นส่วนหนึ่งของโปรเจกต์ใหญ่ ให้รันผ่านคำสั่ง Terminal ดังนี้:

```bash
flutter run -t lib/learning/menu/main.dart
```

หรือถ้าใช้ VS Code:
1. เปิดไฟล์ `lib/learning/menu/main.dart`
2. กด `F5` หรือเลือก **Start Debugging**

## 📂 โครงสร้างไฟล์
- `main.dart`: โค้ดหลักทั้งหมด รวมทั้ง UI และข้อมูลจำลอง (Mock Data)

> **Note**: โปรเจกต์นี้มีการเรียกใช้รูปภาพจากโฟลเดอร์ `images/` ดังนั้นต้องแน่ใจว่าได้ระบุ assets ไว้ใน `pubspec.yaml` ที่ root project แล้ว
