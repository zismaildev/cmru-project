# Assignment 2: Control Flow (Loops)

## Overview
แบบฝึกหัดการใช้ Loop และเงื่อนไข (Conditionals) ในภาษา Dart

## Code Deep Dive

### 1. [`lab1.dart`](./lab1.dart)
**Topic:** Basic For Loop
- รับค่าจำนวนรอบ `n` จากผู้ใช้
- วนลูปพิมพ์ข้อความ "รอบที่ x" ตั้งแต่ 1 ถึง `n`

### 2. [`lab2.dart`](./lab2.dart)
**Topic:** Even Number Finder
- รับค่า `n` จากผู้ใช้
- วนลูป 1 ถึง `n` และใช้ `if (i % 2 == 0)` เพื่อหาเลขคู่
- แสดงผลเฉพาะเลขคู่ทางหน้าจอ

### Other Labs
- **lab3.dart - lab5.dart**: แบบฝึกหัดเพิ่มเติมเกี่ยวกับการวนซ้ำและตรรกะพื้นฐาน
