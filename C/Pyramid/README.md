# 🔺 Pyramid (Basic)

โปรแกรมพื้นฐานสำหรับการสร้างรูปสามเหลี่ยมพีระมิดแบบง่าย

## 💻 การทำงาน (Logic)
- ใช้ Loop ซ้อนกัน 2 ชั้น
- ชั้นแรก: วนลูปตามจำนวนแถว
- ชั้นสอง: วนลูปเพื่อพิมพ์ช่องว่างและพิมพ์ดาว

## 📝 ไฟล์ประกอบ (Files)
- **[peramid.c](./peramid.c)**: Source code หลัก

> **Note**: โค้ดนี้ใช้ `getch()` ซึ่งอาจต้องใช้ Library `conio.h` (บน Windows บาง Compiler) หากคอมไพล์ไม่ผ่านสามารถลบ `getch()` ออกได้
