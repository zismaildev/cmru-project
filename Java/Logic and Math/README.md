# 🧮 Logic & Math

รวมแบบฝึกหัดพื้นฐานการใช้ Logic และคณิตศาสตร์ใน Java

## 📜 รายการโปรแกรม
1. **DivisibleNumbers (`DivisibleNumbers.java`)**
   - โปรแกรมหาตัวเลข 1-100 ที่หารด้วย 3 และ 5 ลงตัว
   - แสดงตัวอย่างการใช้ Loop 3 แบบ (`for`, `while`, `do-while`) เปรียบเทียบกัน
2. **NumberStats (`NumberStats.java`)**
   - โปรแกรมรับค่าตัวเลขจำนวนมากจนกว่าผู้ใช้จะหยุด (กด Enter เปล่า)
   - คำนวณสถิติ: ผลรวม (Sum), ค่าเฉลี่ย (Average), แยกจำนวนคู่/คี่
3. **Switch Calculator (`Switch2.java`)**
   - เครื่องคิดเลขพื้นฐาน (+, -, *, /) โดยใช้ `switch-case`

## 🚀 ผลลัพธ์ (Key Concepts)
- **Control Flow**: `if-else`, `switch-case`
- **Loops**: `for`, `while`, `do-while`
- **Input Handling**: `Scanner`, `BufferedReader`
- **Data Collections**: `ArrayList`
