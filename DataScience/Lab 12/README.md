# 📋 Lab 12: List

บทเรียนเจาะลึกเรื่อง List ซึ่งเป็นโครงสร้างข้อมูลที่ยืดหยุ่นและใช้งานบ่อยที่สุด

## 📚 เนื้อหา (Topics)
- **Properties**: Ordered (มีลำดับ), Mutable (แก้ไขได้), Allow Duplicates
- **Slicing**: การตัด List (`list[start:end]`)
- **Operations**: `+` (รวม List), `len()` (นับจำนวน), `sum()` (หาผลรวม)
- **Methods**:
  - `.append()`: เพิ่มสมาชิกต่อท้าย
  - `.sort()`: เรียงลำดับ (ปกติและย้อนกลับ `reverse=True`)
  - `.pop()`: ลบสมาชิกออก
- **Data Analysis**: การหาค่าเฉลี่ย (Mean) ด้วย For Loop (ก่อนใช้ Library)

## 📝 ไฟล์ประกอบ
- **[lab_12_list.py](./lab_12_list.py)**: โค้ดตัวอย่างการจัดการ List แบบครบวงจร
