# 📖 Lab 14: Dictionary

บทเรียนเรื่อง Dictionary สำหรับเก็บข้อมูลแบบ Key-Value Pair

## 📚 เนื้อหา (Topics)
- **Structure**: `{ key: value }`
- **Accessing**: การเข้าถึงข้อมูลด้วย Key (`dict['key']`)
- **Looping**: การวนลูป `.keys()`, `.values()`, `.items()`
- **Methods**:
  - `.update()`: เพิ่มหรือแก้ไขข้อมูล
  - `.pop()`, `.popitem()`: ลบข้อมูล
  - `.clear()`: ล้างข้อมูลทั้งหมด
- **Properties**: Python 3.7+ เป็น Ordered, Keys ต้องไม่ซ้ำกัน (Unique)

## 📝 ไฟล์ประกอบ
- **[lab_14_dictionary.py](./lab_14_dictionary.py)**: โค้ดตัวอย่างการจัดการ Dictionary
