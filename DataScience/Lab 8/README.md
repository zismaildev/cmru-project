# 🛠️ Lab 8: Function vs Method

บทเรียนเปรียบเทียบความแตกต่างระหว่าง Function ทั่วไปและ Method ของ Object

## 📚 เนื้อหา (Topics)
- **Method Definition**: ฟังก์ชันที่ผูกติดกับ Object (เรียกใช้ด้วย `.`)
- **String Methods**:
  - `.upper()`, `.lower()`: แปลงตัวพิมพ์ใหญ่/เล็ก
  - `.strip()`: ลบช่องว่างหัวท้าย
  - `.count()`: นับจำนวนคำที่ปรากฏ
  - `.split()`: แยกข้อความออกเป็น List
- **Dir()**: การใช้ `dir(object)` เพื่อดู Method ทั้งหมดที่มีให้ใช้

## 📝 ไฟล์ประกอบ
- **[lab_8_function_vs_method.py](./lab_8_function_vs_method.py)**: โค้ดตัวอย่างการใช้ String Methods
